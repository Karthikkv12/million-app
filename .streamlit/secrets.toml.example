# Example Streamlit secrets file (DO NOT commit real secrets).
# Copy this to `.streamlit/secrets.toml` and fill real values locally.

# Use a managed Postgres for persistence. Example:
DATABASE_URL = "postgresql://db_user:supersecretpassword@db.example.com:5432/million_db"

# Optional: separate test DB for CI
# TEST_DATABASE_URL = "postgresql://ci_user:ci_pass@ci.db.example.com:5432/million_test"

# Optional SMTP settings (if you add email features)
# SMTP_URL = "smtp://smtp_user:password@smtp.example.com:587"
